server:
  port: 8080

spring:
  kafka:
    bootstrap-service: ${KAFKA_BROCKER:localhost:9092}
    consumer:
      group-id: orchestrator-group
      auto-offset-reset: latest
logging:
  level:
    org:
      apache:
        kafka: OFF
